# Hermes.md — AI 上下文契约（优化版）

> 本文件是 AI 进入本项目的第一入口。
> AI 既是**读者**也是**作者**。
> 读完的 AI 必须在离开前，把变化写回正确位置，否则视为**任务未完成**。

---

## 📋 目录

**核心规范（必须阅读）**

- [§0 文档所有权规则](#0-文档所有权规则三类知识分层管理) — 三类知识分层管理，明确AI与人类的职责边界
- [§1 写入触发器](#1-写入触发器什么时候必须写) — 什么时候必须写文档，避免过度文档化
- [§2 文档维护协议](#2-文档维护协议怎么写) — 5步流程确保文档质量
- [§3 编辑原则](#3-编辑原则借鉴洁癖skill) — 减优于加、合并优于追加等9条核心原则
- [§4 默认读取集](#4-默认读取集启动必读) — 启动时必须读取的文件清单
- [§5 任务型读取与写入](#5-任务型读取与写入按需) — 按任务类型定义读写内容
- [§6 永不读取](#6-永不读取默认屏蔽) — 默认屏蔽的目录和文件类型
- [§7 上下文自检清单](#7-上下文自检清单每次任务结束前必跑) — 确保质量的检查机制

**约束与标准**

- [§8 文档长度预算](#8-文档长度预算硬约束) — 硬性行数上限，防止膨胀
- [§9 归档协议](#9-归档协议什么进archive) — 什么内容应该归档
- [§10 失败上报协议](#10-失败上报协议) — 发现问题时的处理流程
- [§11 一句话总结](#11-一句话总结) — 核心理念速记

**结构与模板**

- [§12 标准文件树模板](#12-标准文件树模板通用软件工程) — 统一的目录结构
- [§13 毕业机制](#13-毕业机制稳定知识升级通道) — 稳定知识从memory升级到docs

**高级功能**

- [§14 特殊情况处理](#14-特殊情况处理) — 各种场景的处理指南
- [§15 完整执行流程总结](#15-完整执行流程总结) — 5步流程的完整指南
- [§16 参考资料](#16-参考资料) — 相关资源链接
- [§17 规范演化机制](#17-规范演化机制突破性见解) — 动态分层、渐进触发、分级自检

**快速导航**

- **新AI首次阅读**：§0 → §1 → §4 → §7
- **执行文档更新**：§1 → §2 → §7（按需读取其他章节）
- **遇到问题时**：§10 → §14 → §15
- **优化规范时**：§17（规范演化机制）

---

## 0. 文档所有权规则（三类知识分层管理）

**核心理念**：不同类型的文档服务于不同的受众，必须严格区分职责，避免信息混乱。

| 角色                         | 写什么                                     | 不写什么                     | 受众                   |
| ---------------------------- | ------------------------------------------ | ---------------------------- | ---------------------- |
| **人类（项目所有者）** | 决策、价值观、长期愿景、不可逆配置         | 进度、状态、细节实现         | 项目团队               |
| **AI（任何会话）**     | 当前事实、项目地图、已知问题、架构变更记录 | 商业决策、用户偏好、模糊猜测 | 下次会话的AI、人类同事 |
| **Git**                | 所有变更的时间戳与差异                     | —                           | 所有协作者             |

**三类知识分层**（借鉴洁癖skill）：

1. **Agent记忆系统**（若agent支持）：

   - **位置**：`~/.claude/projects/<...>/memory/` 或等效路径
   - **受众**：Agent自己跨会话复用
   - **职责**：个人偏好、非显而易见的项目事实、跨项目reference
   - **不同步的代价**：下次会话Agent忘记历史决策
2. **项目根文档**（Hermes.md / CLAUDE.md / AGENTS.md）：

   - **位置**：项目根目录
   - **受众**：当前项目里的AI（下次会话自己）
   - **职责**：项目约定、结构、红线、环境变量、路由清单
   - **不同步的代价**：下次AI在这个项目里走弯路
3. **项目文档目录**（docs/ + README.md）：

   - **位置**：`docs/`目录
   - **受众**：其他人（人类同事、下游开发者、未来接手的AI）
   - **职责**：接入指南、架构图、运维手册、交接说明、API参考
   - **不同步的代价**：其他人或系统无法正确接入或运维

**铁律**：AI不允许写自己**没有100%证据**的事实。证据 = 代码行号 / 命令输出 / git log。

---

## 1. 写入触发器（什么时候必须写）

AI在以下任一事件发生时，**必须**在任务结束前执行"文档同步动作"：

| 触发事件                                       | 写入位置                         | 写入内容                 |
| ---------------------------------------------- | -------------------------------- | ------------------------ |
| 完成一个功能 / 修复一个bug                     | `docs/current-status.md`       | 更新"能用的"或"坏的"区块 |
| 新建 / 删除 / 重命名目录                       | `docs/project-map.md`          | 更新目录表               |
| 模块边界、依赖方向、数据流发生变化             | `docs/current-architecture.md` | 增量改写对应章节         |
| 发现一个**当前有效**的bug / 风险         | `docs/known-issues.md`         | 新增条目 + 优先级        |
| 任何过时 / 错误 / 矛盾的信息                   | 原地修改并更新 `last_verified` | 不允许"追加"式补丁       |
| 做出一个**长期决策**（如选型、目录结构） | `docs/decisions/ADR-XXX.md`    | 完整ADR记录（见§4）     |

**没有触发器 = 没有写入动作**。禁止AI主动"润色"或"完善"未发生变化的文档。

**变更影响矩阵**（借鉴洁癖skill）：

常见变更模式与需要更新的文档映射：

- **新增API / 路由** → Hermes.md路由清单 + integration-guide + architecture的Routes
- **新增/改名环境变量** → Hermes.md环境变量表 + runbook + 下游integration-guide
- **新增数据库表** → Hermes.md + architecture的Data Model
- **新增大特性（跨多文件）** → 以上全部 + architecture新章节 + handoff已完成清单
- **跨项目改动** → 上下游两边的docs**都要对齐**（最常见的漏改场景）

---

## 2. 文档维护协议（怎么写）

每次写入必须按以下顺序执行，**禁止跳步**（借鉴洁癖skill的5步流程）：

### Step 1: 读现状（盘点现状）

- 用 `read_file` 读取**目标文档的当前完整内容**
- 确认 `last_verified` 字段、当前行数、是否已超 `max_lines`
- **强制机械式枚举**：列出所有相关文件，对每个文件标「评估过/要改/不用改」

### Step 2: 计算差异（识别变更）

- 明确列出："哪些行要删、哪些要改、哪些要加"
- 给出**新旧对比表**（哪怕只有3行）
- **用变更影响矩阵思考**：这次变更会波及哪些文档层级？

### Step 3: 评估长度（尺寸体检）

- 写入后总行数 ≤ `max_lines`？
  - ✅ 是 → 执行写入
  - ❌ 否 → 先压缩最旧的20%，再写
- **额外做一次「体量倒挂」体检**：`du -sh <memory目录>` 对比 `du -sh docs/`
  - **健康态是docs厚、memory薄**——docs是沉淀的权威层，memory是流动的「最近教训+指针」层
  - 若memory反而比docs大，几乎一定是「本该毕业进docs的稳定知识还赖在松散记忆文件里」

### Step 4: 写入（实际修改）

- 整段替换，不要在文档末尾"append"
- 同步更新 `last_verified: YYYY-MM-DD` 字段
- **必须真的用工具修改文件**，"我会怎么改"的描述不算完成

### Step 5: 自检（见§6）

---

## 3. 编辑原则（借鉴洁癖skill）

**减优于加**（最重要）：

- 每次同步动作结束后，Hermes.md净涨幅 > 30行就是红灯——很可能在写历史叙事而不是补规则
- 回头审：这条加的是"下次AI写代码时必须看到"的规则，还是"上次会话告诉下次会话发生了什么"的便条？

**合并优于追加**：

- 新信息是对旧信息的更新，改旧条目
- 加新条目前先grep同关键字，看现有条目能不能并

**删除优于保留**：

- 完成的临时计划、推翻的决策、已被新版本取代的项目记忆、单次事故的流水账复盘——删

**毕业优于内部挪腾**（针对memory）：

- 一条记忆稳定、复用、或本属「系统怎么工作」时，别在memory里搬来搬去——并进docs/Hermes.md，原文件缩成一行指针或删
- 这是把memory压回「薄」的唯一治本手段（见§12毕业机制）

**精确优于冗长**：

- 一条记忆说清楚一件事，别塞三件

**绝对时间**：

- 永远 `2026-04-29`，不写"今天"、"最近"

**面向读者**：

- docs/的读者是"第一次接触这个项目的外部人"，写的时候想象对方只有5分钟能看完

**受众不混**：

- Hermes.md里不抄docs/的全文，docs/里不写"我记得上次……"——这是记忆的事

**指针不重复**：

- 同一条事实如果docs/里已详写，Hermes.md只在「深入文档」指针表里出现一次，不在概览段再叙事一次

---

## 4. 默认读取集（启动必读）

进入任何任务前，按顺序读取：

1. 本文件（`Hermes.md`）
2. `docs/current-status.md` — 当前事实快照
3. `docs/project-map.md` — 项目地图

读取方式：使用 `@file` 引用或 `read_file` 工具，**不要全目录扫**。

**漏读后果**：必读文件未读 → 可能基于错误前提做决策 → 触发§9失败上报协议。"已读"必须在会话回复中明确声明（含文件+行号引用）。

---

## 5. 任务型读取与写入（按需）

| 任务类型        | 读取                                                  | 可能写入                             |
| --------------- | ----------------------------------------------------- | ------------------------------------ |
| 架构 / 模块设计 | `docs/current-architecture.md`, `docs/decisions/` | `current-architecture.md`, 新增ADR |
| Bug修复 / 排障  | `docs/known-issues.md`, `docs/debug/<日期>.md`    | `known-issues.md`（修复后）        |
| 协议 / 接口变更 | `docs/protocols/*.md`                               | 对应协议文件 + 新增ADR               |
| 新功能 / 重构   | `docs/decisions/`                                   | `current-status.md`, 可能新增ADR   |

### ADR（架构决策记录）模板

```markdown
# ADR-XXX: <一句话标题>

- **状态**：proposed / accepted / deprecated
- **日期**：YYYY-MM-DD
- **触发者**：<人类决策 / AI建议 + 人类确认>
- **背景**：为什么要做这个决策？
- **选项**：考虑过哪些方案？
- **决策**：最终选了什么？
- **后果**：会带来什么约束 / 代价？
- **撤销条件**：什么情况下应该回滚？
```

**ADRs是只增不删的**，但状态可以改为 `deprecated`，并指向替代方案。

---

## 6. 永不读取（默认屏蔽）

- `build/`, `dist/`, `out/`, `node_modules/`, `.venv/`, `__pycache__/`
- `logs/`, `*.log`, `*.log.*`
- `docs/archive/`, `docs/debug/<历史日期>.md`（**当前会话调试中的除外**）
- 任何 `Last verified` 字段超过30天的文档
- 任何超过自身行数上限的文档（见§7）

---

## 7. 上下文自检清单（每次任务结束前必跑）

这一步同时防止"漏改docs" + "误把叙事塞进Hermes.md"。改完后逐条检查：

**读取侧：**

- [ ] 我只读了我声明要读的文件（没有偷偷扫目录）
- [ ] 我引用的每个事实，都能追溯到具体文件 + 行号
- [ ] 我没有引用 `docs/archive/`、`build/`、`logs/` 里的内容

**写入侧：**

- [ ] 本次任务触发了写入事件吗？→ 是：我执行了§2的完整5步
- [ ] 我没有把"猜测"包装成"事实"
- [ ] 我修改的所有文档都更新了 `last_verified`
- [ ] 我没有让任何文档超过 `max_lines`
- [ ] 我没有append，只做了整段替换
- [ ] 所有时间都是**绝对时间**（YYYY-MM-DD），无"今天/最近/刚刚"等相对词
- [ ] Hermes.md净涨幅 ≤ 30行；超了说明在写叙事，回去精简（§12）

**尺寸/反膨胀（先查这组，不达标的话回头先精简）：**

- [ ] Hermes.md净涨幅 ≤ 30行（超了就是塞了历史叙事，回去删/迁docs）
- [ ] 没新增"X起Y上线，详见docs/Z.md"这种blockquote历史叙事条目
- [ ] 没在Hermes.md里抄docs/已有的详细机制说明
- [ ] 单条memory文件没超~100行（超了拆/删/改成reference）
- [ ] **记忆索引 `MEMORY.md` ≤ 25KB且≤ 200行**（`wc -c`实测；超出部分会话开始时静默不加载=等于没记）
- [ ] **体量没倒挂**：`du memory` 不应大于 `du docs/`；倒挂了说明有该毕业进docs的知识赖在memory，回去毕业

**完整性/反漏改（再查这组）：**

- [ ] 第一步列出的每个文件，都判断了"不用改"或"已改"
- [ ] 记忆索引（若有）里的每个链接指向存在的文件
- [ ] 每个记忆文件的description和内容对得上
- [ ] 记忆之间没有互相矛盾
- [ ] Hermes.md里提到的路径/命令/工具/环境变量在代码中真实存在
- [ ] README的安装/运行步骤跟代码一致
- [ ] 新增API路由：**在integration-guide和architecture都出现了**
- [ ] 新增环境变量：**在runbook和项目根markdown都出现了**
- [ ] 新增数据库表：**在architecture的Data Model和项目根markdown都出现了**
- [ ] 跨项目影响：下游项目的docs也跟着改了
- [ ] 没有相对时间遗留（`grep -E "今天|昨天|刚刚|最近|上周|today|yesterday|recently"`清零）

**任一项未通过 = 任务未完成，禁止输出最终结论。**

---

## 8. 文档长度预算（硬约束）

| 文件                             | 用途          | 上限行数  | 默认读取 | 写入者             |
| -------------------------------- | ------------- | --------- | -------- | ------------------ |
| `Hermes.md`                    | AI入口规则    | 300 / 500 | ✅       | 人类（AI只能建议） |
| `docs/current-status.md`       | 当前事实快照  | 120       | ✅       | AI                 |
| `docs/project-map.md`          | 项目地图      | 120       | ✅       | AI                 |
| `docs/current-architecture.md` | 架构解释      | 200       | ❌ 按需  | AI                 |
| `docs/known-issues.md`         | 当前有效问题  | 150       | ❌ 按需  | AI                 |
| `docs/decisions/ADR-XXX.md`    | 架构决策记录  | 100/篇    | ❌ 按需  | 人类决策+AI起草    |
| `docs/protocols/*.md`          | 协议/接口规范 | 150/篇    | ❌ 按需  | AI                 |
| `docs/archive/**`              | 历史快照      | 无上限    | ❌ 禁用  | AI（自动归档）     |

**超过上限 = 违规**。`300 / 500`格式 = 软上限/硬上限：超过软上限应启动精简（见§12毕业机制），超过硬上限必须拆分或归档到 `docs/archive/`，不允许无限加内容。

**额外尺寸体检**（借鉴洁癖skill）：

| 文件                          | 上限                              | 超过怎么办                                                                                                         |
| ----------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `Hermes.md` / `AGENTS.md` | ~300行 / ~15KB（软，看adherence） | 先精简：扫顶部blockquote/历史叙事段→删/迁docs；项目概览只留1-3行+速查表                                           |
| 记忆索引 `MEMORY.md`        | **≤200行且≤25KB（硬）**   | Claude Code只加载 `MEMORY.md`的前200行或前25KB（先到先算），**超出部分在会话开始时静默不加载——等于没记** |
| 单条memory文件                | ~100行（软）                      | 通常在塞多件事/写成事故复盘→拆/删；若是稳定机制说明，提升进docs再把记忆缩成reference指针                          |
| `docs/<single>.md`          | ~1500行（软）                     | 切分成多文件，加目录索引                                                                                           |

---

## 9. 归档协议（什么进archive）

以下内容**必须**进 `docs/archive/`，并在原位置删除或留一行指针：

```markdown
> ⚠️ 本节已过期，请看 `docs/archive/2026-06-16-bug-fix.md`
```

- 已修复的bug
- 已废弃的方案
- 超过30天未更新的草稿
- 调试记录（保留7天后归档）
- 任何超过2次 `last_verified` 未更新的文档

归档命名规则：`YYYY-MM-DD-<主题>.md`

---

## 10. 失败上报协议

发现以下情况，**立即停止任务并上报**，禁止"边干边修"：

- 必读文件缺失或损坏
- 必读文件超过长度上限
- 多个文档对同一事实描述矛盾
- 核心目录结构与 `docs/project-map.md` 严重不符
- 任务完成但**没有触发任何写入事件**（意味着AI没识别出变化）

---

## 11. 一句话总结

> **读要省，写要准，留痕要短。**
> 每次离开前，把世界变动的部分写回小入口，让下一个AI 30秒内进入状态。

---

## 12. 标准文件树模板（通用软件工程）

> 本节是**目录骨架锚点**（单一来源）。技术栈差异按需在 `docs/file-tree-template.md` 扩展。

```text
project-root/
├── .github/{workflows, ISSUE_TEMPLATE, PULL_REQUEST_TEMPLATE.md}
├── docs/{current-status, project-map, current-architecture, known-issues}.md
│   ├── decisions/   # 架构决策记录（ADR）
│   ├── protocols/   # 接口协议
│   └── archive/     # 历史快照
├── src/{components, services, models, utils, config}/
├── tests/{unit, integration, e2e}/
├── scripts/  config/  .vscode/  .editorconfig
├── .gitignore  .env.example  LICENSE
├── README.md  CHANGELOG.md  CONTRIBUTING.md
├── package.json   # 或 pyproject.toml / Cargo.toml / go.mod
└── Hermes.md
```

**简注**：`.github/` 含CI；`docs/` 与本契约配合；`src/` 按栈差异（前端components，后端services/models）；`tests/` 三层金字塔。

### 12.1 指针引用义务（强制规则）

未来AI在**新建或更新**以下文档时，**必须**在文档首部添加指针，指向本节：

| 文档                             | 触发场景        |
| -------------------------------- | --------------- |
| `docs/current-architecture.md` | 新增/重构模块时 |
| `docs/decisions/ADR-XXX.md`    | 新增ADR时       |
| `docs/protocols/*.md`          | 新增接口协议时  |
| 任何新模块的README               | 首次创建模块时  |

指针固定格式：

```markdown
> 📂 目录结构参见 [§12 标准文件树模板](#12-标准文件树模板通用软件工程)
```

**目的**：目录事实**单一来源**集中到本节。**反作弊**：添加指针的提交应在git commit message中标注 `add-pointer-ref`（见§6）。

---

## 13. 毕业机制（稳定知识升级通道）

**核心矛盾**：Hermes.md倾向持续追加（AI教训、用户偏好、新模式），但**规则手册不是变更日志**——半年后顶部堆满历史叙事，把真正的规则挤出adherence。

**记忆只增不改、docs就地编辑——要靠「毕业」机制把知识往上泵（膨胀头号根因）**：

必须理解这条不对称，否则记忆永远在膨胀：**docs靠就地编辑收敛**（系统改10次，还是那一份 `ARCHITECTURE.md`），**而agent记忆天生只追加**（每条教训生一个新文件，旧的不删）。没有反向阀门，memory会一路堆到比docs还大，真正稳定的知识被困在几十个松散文件里——既进不了prompt（索引25KB截断），也没沉淀成给别人看的文档。

**反向阀门 = 毕业（promote）**。一条记忆满足下面任一条，就把它「毕业」：内容并进对应的 `docs/` 或 `Hermes.md`，然后**把原记忆文件删掉或缩成一行指针**：

1. **同一主题的教训反复出现到第3次** → 它已是稳定知识而非「最近踩的坑」，归docs
2. **它讲的是「系统怎么工作」而非「我们踩过什么坑/做过什么决策」** → 本就是docs的职责，memory顶多留指针
3. **它是「X上线/落地/就位」的事件记录** → 现役事实进docs，过程进git log / `docs/CHANGES.md`，memory不留常驻文件
4. **它是详细机制说明（> 10行）** → 归docs/，本节只留指针

判据一句话：**「下一个接手的人（不只是我自己）需要知道这件事吗？」需要 → 它属于docs，不是memory。**

**记忆文件类型前缀生命周期**（若使用）：

- `reference_`：通常合法长期常驻
- `feedback_`：稳定后毕业
- `project_`：多数是事件记录，**是优先毕业/删除的对象**——决策结论进docs，过程进changelog

**CLAUDE.md / AGENTS.md是规则手册，不是变更日志**（重要）：

最常见的翻车模式：每次开发完都在Hermes.md顶部加一段blockquote历史叙事——"2026-05-08 X功能上线，详见docs/Y.md"。一次很爽，半年后顶部就是200行blockquote把真正的规则推到看不见。**这种叙事不属于Hermes.md**，它的归宿是git log / `/changelog` 页 / `docs/CHANGES.md`。

判断一条信息该不该进Hermes.md，问一句：**下次AI写代码时如果没看到这条，会不会犯错？**

| 例子                                                        | 进Hermes.md？ | 理由                                                                    |
| ----------------------------------------------------------- | ------------- | ----------------------------------------------------------------------- |
| "Prisma查询只写在 `modules/**/data/`"                     | ✅            | 违反就是边界破坏，AI必须看到                                            |
| "rsync单文件部署必须用完整target路径"                       | ✅            | 踩坑警示，会再次踩                                                      |
| "禁止裸跑systemctl stop aihot-worker"                       | ✅            | 红线，事故级                                                            |
| "2026-05-08 timelineAt上线，详见docs/ARCHITECTURE.md §5.4" | ❌            | 详细机制在docs；AI改到这块自然会读docs；「深入文档」指针表已做这件事    |
| "2026-04-30起公网开放，匿名可访/、/all"                     | ❌            | 既是历史也是事实，但事实归docs/ARCHITECTURE.md §8 + 项目概览一句话足矣 |
| "5/8修了X bug的复盘细节"                                    | ❌            | 单次事故记忆，归memory或干脆删                                          |

✅ 该进Hermes.md的内容：硬边界规则、禁止事项、命令速查、权限模型、协作流程、深入文档指针表、踩坑警示。
❌ 不该进的：历史叙事（"X时刻起Y上线"）、详细机制说明、单次事故复盘、bug fix流水账、"详见docs/Z.md"的指针句子（这个角色已经被「深入文档」指针表占掉了）。

**反膨胀优先级最高**：补漏前先毕业。每次会话Hermes.md净涨幅 > 30行即红灯——回头审：这条加的是规则还是历史叙事？

**执行顺序**：先精简（破除膨胀）→ 再做本次会话增量同步。混着做会两头不到位。

---

## 14. 特殊情况处理

**项目还没有README或Hermes.md**：判断项目是不是到了"有可运行代码"的阶段。是→创建。还在vibe阶段→跳过，但在摘要里提一句。

**对话没有产生新事实**：审查现有记忆和文档有没有过期/冲突/相对时间——审查本身就有价值。

**记忆之间出现无法自动判断的矛盾**：列在「未处理」让用户决定。**这是唯一需要用户介入的情况**，其他都自己拍板。

**跨项目改动**：本次对话改了多个项目，每个项目都要跑一次完整的第一步（ls+读docs）。不要假设一个项目的docs改了，另一个就不用。尤其是上游-下游对接文档（集成指南/SDK说明/API协议），两边都要对齐。

**发现之前的同步漏了东西**：修掉。不要说"那不是这次对话的事"——你就是这个项目的持续编辑，过去的漏洞也归你管。

**全局配置极度克制**：`~/.claude/CLAUDE.md` / `~/.codex/AGENTS.md` 只有用户在对话中明确表达了**跨项目的核心原则**才动。日常项目细节绝不进全局。

---

## 15. 完整执行流程总结

### 第零步：尺寸体检（防膨胀）

任何同步动作之前，先 `wc -l` 关键文件（见§8额外尺寸体检）。

**超尺寸是这个skill的最高优先级，大于"补本次会话漏掉的同步"**。原因：`MEMORY.md`超25KB的部分根本不进上下文（静默丢失），超尺寸的Hermes.md让真正的规则被叙事段挤出adherence——两种情况下，同步再补都徒劳。

**执行顺序**：先精简（破除膨胀）→ 再做本次会话增量同步（补漏）。两件事不能合并——精简时心态是"什么不该在这"，补漏时心态是"什么该补到这"，混着做会两头不到位。

### 第一步：盘点现状（强制机械式枚举，不能跳过）

**先做ls，再做判断。**

1. 列出agent的记忆文件（如有）
2. 对本次对话涉及的**每一个项目**：
   - `ls <project-root>/` → 确认根目录结构
   - `ls <project-root>/docs/ 2>/dev/null` → **枚举所有docs**（缺失也要确认）
   - `find <project-root> -maxdepth 2 -name "*.md" -not -path "*/node_modules/*" -not -path "*/.git/*"` → 兜底抓散落的.md
   - 读 `README.md`、`Hermes.md` / `CLAUDE.md` / `AGENTS.md`、每一个 `docs/*.md`
3. 读全局agent配置（若有）
4. 回顾本次对话全部内容

**输出一张文件清单**（内部用，不用给用户看），对每个文件标：「评估过/要改/不用改」。**漏一个不行**——这是这个skill最容易翻车的地方。

### 第二步：识别变更——用"变更影响矩阵"思考

**不要只看对话增量有什么新事实，要看新事实会波及哪些文档层级。**

**关键检查**：这次对话是不是**跨项目**的？如果改了项目A且项目B依赖它（通过SDK、API、子域、环境变量），**项目B的docs也要改**。这是历次同步最常翻的车。

### 第三步：实际修改（用工具，不只是描述）

你必须**真的用Edit修改现有文件、用Write创建新文件、用删除命令清理废弃文件**。"我会怎么改"的描述不算完成。

**顺序建议**：先改docs/（改错影响外部）→ 再改Hermes.md/AGENTS.md → 最后理记忆。先动外部优先级最高的，即使中途被打断，读者看到的也是对齐的最新状态。

**docs/编辑要点**——新增一个能力的文档变更通常要四处都补：

1. **integration-guide** 或对应"外部视角"文档：加**怎么用**（curl/SDK示例/错误码表）
2. **architecture**：加**怎么工作**（数据流、状态机、设计取舍）
3. **runbook**：加**怎么运维**（冒烟命令、故障排查、环境变量）
4. **handoff** 或CHANGELOG：加**已完成**

API速查表、环境变量表、术语表是高频查询的结构化信息，**必须保持"所见即最新"**。

### 第四步：自检清单（必须逐项过一遍）

见§7完整自检清单。

### 第五步：变更摘要

在所有文件修改完之后（不是之前），给用户简洁摘要：

```
## 同步完成

### 记忆变更
- 更新：xxx（原因）
- 新增：xxx
- 删除：xxx（原因）

### 文档变更（按项目分组，每个项目列全改动的文件）
- <项目A>/Hermes.md — xxx
- <项目A>/docs/integration-guide.md — xxx
- <项目A>/docs/architecture.md — xxx
- <项目B>/docs/<integration>.md — xxx

### 未处理
- xxx（为什么没处理，比如需要用户确认）
```

只列有实际变更的条目。没改的不写。

---

## 16. 参考资料

- **洁癖skill** — 知识库清理与同步的最佳实践
- **变更影响矩阵** — 完整的"变更类型→要改哪些文件"映射表
- **Agent路径速查** — Claude Code / Codex / OpenCode各自的记忆与配置路径

---

## 17. 规范演化机制（突破性见解）

**核心理念**：规范不是静态规则，而是**活的系统**，需要随项目实践不断演化和优化。

### 17.1 动态分层模型（适应项目阶段）

**问题**：静态的三类分层无法适应项目演化的不同阶段。

**解决方案**：引入"知识成熟度"概念，建立动态分层模型：

| 项目阶段               | 特征                 | 文档策略                             | 规范严格度       |
| ---------------------- | -------------------- | ------------------------------------ | ---------------- |
| **L0（探索期）** | 快速试错、频繁变更   | 所有知识集中在memory，快速迭代       | 低（仅基础自检） |
| **L1（成长期）** | 功能稳定、开始文档化 | 重要知识开始毕业到docs，建立基础文档 | 中（标准流程）   |
| **L2（成熟期）** | 系统稳定、需要维护   | 严格分层，启用完整的规范机制         | 高（完整规范）   |

**执行指南**：

1. **阶段识别**：根据以下指标判断项目阶段

   - 代码提交频率（高→L0，中→L1，低→L2）
   - 文档与代码比例（低→L0，中→L1，高→L2）
   - 团队规模变化（快速增长→L0，稳定→L1，缩减→L2）
2. **策略调整**：

   - **L0阶段**：放宽文档长度限制，简化自检流程，专注于记录关键决策
   - **L1阶段**：开始执行毕业机制，建立标准文档结构
   - **L2阶段**：完整执行所有规范要求，建立自动化检查

**优势**：规范可以根据项目阶段自动调整严格程度，避免早期项目的过度文档化。

### 17.2 渐进式触发机制（智能学习型触发器）

**问题**：触发式写入可能遗漏重要但未列出的事件类型。

**解决方案**：建立"触发器学习机制"：

#### 第一阶段：基础触发器（当前规范）

- 使用规范§1中的触发事件列表
- 记录所有"疑似应该写文档但未触发"的情况

#### 第二阶段：学习与扩展

1. **记录疑似事件**：

   ```markdown
   ## 疑似触发事件记录（内部使用）
   - 日期：YYYY-MM-DD
   - 事件描述：xxx
   - 当时是否写文档：是/否
   - 事后评估：应该写/不应该写
   - 原因：xxx
   ```
2. **定期分析**（每2-4周）：

   - 统计疑似事件类型和频率
   - 识别高频率且事后评估"应该写"的事件类型
   - 扩展触发器列表
3. **建立项目特定触发器库**：

   ```markdown
   ## 项目特定触发器（追加到规范§1）
   - 性能优化超过20% → 更新architecture的性能章节
   - 安全漏洞修复 → 更新known-issues + 安全文档
   - 第三方API变更 → 更新integration-guide
   ```

#### 第三阶段：智能触发（高级）

- AI自动识别事件重要性
- 基于历史数据预测哪些事件需要文档更新
- 建立"触发置信度"评分机制

**优势**：触发机制可以随项目演化，越来越精准，减少遗漏的同时避免过度触发。

### 17.3 轻量级自检框架（分级自检机制）

**问题**：完整的自检清单对简单任务过于繁琐。

**解决方案**：建立分级自检机制：

#### 🟢 快速自检（<1分钟，3个核心问题）

适用于：简单修改、小bug修复、文档微调

1. **我是否修改了文档？** → 是：更新 `last_verified`
2. **我是否使用了相对时间词？** → 是：改为绝对时间
3. **我是否引用了禁止区域的内容？** → 是：移除引用

#### 🟡 标准自检（3-5分钟，10个关键问题）

适用于：新功能开发、模块重构、文档大改

**读取侧**：

- [ ] 我只读了声明要读的文件
- [ ] 引用的事实都能追溯到具体文件+行号

**写入侧**：

- [ ] 触发了写入事件？→ 执行了完整5步流程
- [ ] 没有把"猜测"包装成"事实"
- [ ] 所有修改的文档都更新了 `last_verified`
- [ ] 没有让文档超过 `max_lines`
- [ ] 没有append，只做了整段替换
- [ ] 所有时间都是绝对时间

**尺寸检查**：

- [ ] Hermes.md净涨幅 ≤ 30行
- [ ] 没有新增历史叙事条目

#### 🔴 深度自检（完整清单，10+分钟）

适用于：重大架构变更、跨项目改动、规范调整

执行规范§7中的完整自检清单，包括：

- 所有读取侧检查
- 所有写入侧检查
- 尺寸/反膨胀检查
- 完整性/反漏改检查

**自检级别选择指南**：

| 任务类型     | 影响范围 | 推荐自检级别 |
| ------------ | -------- | ------------ |
| 修复拼写错误 | 单个文件 | 🟢 快速自检  |
| 修复bug      | 多个文件 | 🟡 标准自检  |
| 新增功能     | 模块级别 | 🟡 标准自检  |
| 架构重构     | 系统级别 | 🔴 深度自检  |
| 跨项目改动   | 多个项目 | 🔴 深度自检  |
| 规范调整     | 全局影响 | 🔴 深度自检  |

**优势**：在保证质量的同时提高效率，适应不同任务复杂度。

### 17.4 规范演化的实施路径

#### 阶段一：基础采用（第1-2周）

1. 采用规范中的核心机制（触发式写入、基本自检）
2. 记录执行中的问题和改进建议
3. 建立项目特定的触发器列表

#### 阶段二：优化调整（第3-4周）

1. 根据记录的问题调整规范
2. 实施分级自检机制
3. 开始执行动态分层模型

#### 阶段三：持续演化（长期）

1. 每月回顾一次规范效果
2. 基于数据优化触发器和自检级别
3. 建立规范维护的自动化工具

### 17.5 规范演化的评估指标

**效率指标**：

- 文档维护时间占比（目标：<10%开发时间）
- 自检通过率（目标：>90%）
- 触发器准确率（目标：>80%）

**质量指标**：

- 文档与代码一致性（目标：>95%）
- 新人上手时间（目标：<2小时）
- AI首次响应正确率（目标：>85%）

**健康指标**：

- 文档膨胀率（目标：净涨幅<5%/月）
- 知识分布平衡度（docs厚度 > memory厚度）
- 触发器覆盖率（目标：>90%关键事件）

### 17.6 规范演化的风险控制

**风险1：过度优化**

- **表现**：频繁修改规范，导致执行混乱
- **控制**：规范修改频率不超过每月1次，每次修改需记录原因

**风险2：规范僵化**

- **表现**：规范长期不更新，无法适应项目变化
- **控制**：每季度强制回顾一次规范，即使不修改也要记录评估结果

**风险3：执行偏差**

- **表现**：不同AI对规范理解不一致
- **控制**：建立规范解释文档，记录常见问题和解答

**总结**：规范演化机制的核心是**让规范服务于项目，而不是项目服务于规范**。通过动态调整、智能学习、分级执行，让规范真正成为提升开发效率的工具，而不是增加负担的枷锁。
